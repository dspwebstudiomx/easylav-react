const HeaderLine = () => {
  return (
    <div className="fixed top-[110px] z-10 h-1 w-full bg-gradient-to-r from-primary to-secondary sm:relative sm:top-0 2xl:fixed 2xl:top-[110px]"></div>
  );
};

export default HeaderLine;
